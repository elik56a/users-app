import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  card: {
    margin: "1rem 2rem",
    textAlign: "center",
  },
  avatar: {
    width: 250,
    height: 250,
    display: "block",
    margin: "0 auto",
  },
  button: {
    display: "block",
    margin: "0 auto",
  },
});

export default useStyles;
